{
  "version": 2,
  "name": "natours",
  "builds": [{ "src": "package.json", "use": "@vercel/node" }],
  "routes": [
    { "src": "/public/(.*)", "dest": "/public/$1" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
